# Test/dev-only extras on top of requirements.txt, for both backend/ and
# scraper/ (each only had pytest before; httpx is backend's, needed by
# FastAPI's TestClient; pytest-asyncio backs the UI test suite's
# NiceGUI `User` fixture, see backend/tests/ui/).
-r requirements.txt
pytest>=8.3,<9.0
httpx>=0.27,<1.0
pytest-asyncio>=0.23,<1.0
